<%= form_with model: [:admin, @reward, @reward_expense], local: true do |f| %>
  <%= render 'shared/error_messages', target: @reward_expense %>
  <%= f.text_field :amount %>
  <%= f.submit %>
<% end %>
