<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_reward_reward_expenses_path(@reward), class: 'section' %>
  <div class="divider">/</div>
  <div class="active section">Add</div>
</div>

<div class="ui segment">
  <%= render 'form' %>
</div>
