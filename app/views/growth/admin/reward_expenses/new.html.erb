<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_reward_expenses_path(reward_id: @reward.id), class: 'section' %>
  <div class="divider">/</div>
  <div class="active section">Add</div>
</div>

<div class="ui segment">
  <%= render 'form' %>
</div>
